let arr = [
    ["a", "b", "c"],
    ["d", "e", "f"],
    ["g", "h", "i"]
];

for (let x = 2; x < arr.length; x--) {
    // for (let y = 0; y < arr[x].length; y++) {
        // console.log(arr[x][y]);
        console.log(arr[x][x]) 
    // }
}
